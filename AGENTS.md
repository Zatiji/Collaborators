# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

## Commits

Never run `git commit` (or `git push`) unless the user explicitly asks for it in that message. Staging/writing code is fine; committing is not the default — always leave it to the user to request.
